"""Reproducible generation of the project's demo video.

Nothing here is part of the system under study: `demo/` only *drives* the
existing environment, policy and GUI and turns them into a film. Building the
video never touches training or evaluation code.

Entry point: ``python -m demo.build --help``.
"""
