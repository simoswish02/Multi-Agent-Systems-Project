% Appendices (~2-3 pages total). \appendix is issued in main.tex.

\section{Configuration and Hyperparameters}
\label{app:config}
\draftpending
% TODO: full configs/default.yaml as tables — environment parameters, DR
% ranges, curriculum phases, agent/training hyperparameters (gamma, n_step,
% buffer size, batch size, target sync, epsilon schedule, LR schedule, ...).

\section{Repository Guide}
\label{app:repo}
\draftpending
% TODO: module map; entry points; how to reproduce: train / eval / sweep
% commands (main.py --mode ..., eval_checkpoints.py,
% testing/evaluate_policy.py); environment setup (conda env, requirements).

\section{Additional Figures}
\label{app:figures}
\draftpending
% TODO: extra plots that do not fit the body (full OFAT grids, extra
% heatmaps, per-phase training curves).
