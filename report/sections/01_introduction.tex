\section{Introduction}
\label{sec:introduction}
\draftpending

% Target length: ~3 pages.
%
% TODO — content plan (see report/planning/OUTLINE.md §1):
% 1. Motivation: cooperative search by drone teams (search & rescue, exploration,
%    surveying); teams beat a single vehicle (coverage, parallelism, redundancy);
%    the hard part is coordinating under uncertainty: partial observability,
%    limited communication, time pressure — and random in-mission faults.
% 2. Problem in one paragraph: N drones, 32x32 occupancy grid with obstacles,
%    one hidden reachable target, fixed step budget; any drone can permanently
%    break at any step; survivors must adapt with no global fault oracle.
% 3. Approach in one paragraph: single shared Dueling Double DQN policy
%    (parameter sharing), per-episode domain randomization + context conditioning
%    -> one policy generalizes across team size / sensing / communication /
%    obstacle density / fault regimes.
% 4. Contributions (bullet list): environment from scratch; decentralized fault
%    model (distress beacon + belief propagation); generalist policy via DR +
%    context; curriculum training pipeline (HPC); MAS-paradigm analysis;
%    fault-robustness experiments; ethical/regulatory analysis.
% 5. Document roadmap.
%
% Figure idea: GUI teaser screenshot — 4 drones, one wreck emitting a beacon.

% TODO: write draft.
