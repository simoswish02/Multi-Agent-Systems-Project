\section{Training Methodology}
\label{sec:training}

% Target length: ~4 pages.
% Figure: training curves (reward, success rate, epsilon/LR schedules) — after
% the final training run completes.

\subsection{Curriculum}
\label{subsec:train-curriculum}
% TODO: ordered phases in config; phases override n_agents / obstacle_density /
% max_steps / fault_prob / DR ranges; n_episodes: 0 skips a phase -> staging by
% config edits, not code; table of the actual phase sequence used for the
% final run.

\subsection{Domain randomization}
\label{subsec:train-dr}
% TODO: per-episode sampling of vision radius, comm range, team size, obstacle
% density, fault probability (final ranges from configs/default.yaml);
% set_domain_params before every reset (coherence between environment and
% context vector).

\subsection{Schedules}
\label{subsec:train-schedules}
% TODO: epsilon-greedy decay (+ per-phase epsilon_reset); LR warmup -> cosine,
% stepped per episode; target sync frequency; replay warm-up.

\subsection{Reinforcement-learning correctness decisions}
\label{subsec:train-correctness}
% TODO (own subsection — this is a strength of the project):
% - truncation != termination (time-outs bootstrap the next-state value);
% - a fault is a truncation, not a terminal: fault is independent of state and
%   action, so zeroing the future value would bias Q-estimates; the broken
%   drone's pending n-step window is flushed immediately WITH bootstrapping;
% - shared_target_reward requires n_step >= 2; drones dead before the find get
%   no team bonus (their windows are already empty);
% - wrecks still consume turns: losing drones buys no extra time;
% - periodic evaluation on a fixed seed set for comparable checkpoint
%   selection (best.pt);
% - BFS auto-nav never runs inside training.

\subsection{Infrastructure and reproducibility}
\label{subsec:train-infra}
% TODO: HPC/SLURM setup (sbatch script, scratch TMPDIR); checkpointing
% (best/last/periodic, resume semantics, replay buffer not serialized);
% TensorBoard logging; seeding (a fixed reset seed reproduces both the layout
% and the fault sequence).

% TODO: write draft.
